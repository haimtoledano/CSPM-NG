version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"   # Access UI at localhost:8080
    environment:
      - API_KEY=${API_KEY}
    volumes:
      - ./data:/app/data # Persist SQLite DB locally
